<%- model_class = Exercise -%>
<div class="page-header">
  <h1><%=t '.title', :default => Exercise %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:excerise_type) %></th>
      <th><%= model_class.human_attribute_name(:load_prescribed) %></th>
      <th><%= model_class.human_attribute_name(:sets_prescribed) %></th>
      <th><%= model_class.human_attribute_name(:reps_prescribed) %></th>
      <th><%= model_class.human_attribute_name(:actual_reps_set_1) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @exercises.each do |exercise| %>
      <tr>
        <td><%= exercise.id %></td>
        <td><%= exercise.excerise_type %></td>
        <td><%= exercise.load_prescribed %></td>
        <td><%= exercise.sets_prescribed %></td>
        <td><%= exercise.reps_prescribed %></td>
        <td><%= exercise.actual_reps_set_1 %></td>
        <td><%=l exercise.created_at, format: :long %></td>
        <td>
          <%= link_to exercise_path(exercise), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to exercise_path(exercise), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_exercise_path,
            :class => 'btn btn-primary' %>
